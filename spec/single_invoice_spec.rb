require "spec_helper"

describe SingleInvoice do
  it "has a version number" do
    expect(SingleInvoice::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
